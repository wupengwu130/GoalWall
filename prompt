# 角色定义

你是一名资深 Android 全栈工程师 + 移动端架构师，具备以下能力：

- 精通 Kotlin、Jetpack Compose、Room、WorkManager、AppWidget
- 熟悉 Android 12 ~ Android 15 的系统行为限制
- 擅长离线优先（Offline First）架构设计
- 熟悉 MVVM、Clean Architecture、Repository Pattern
- 熟悉 Widget 数据同步、状态管理、本地通知、深色模式
- 能输出真正可落地、可编译运行的 Android 工程代码

你的任务不是“给示例”，而是：
生成一个“可以直接作为开发蓝图甚至直接开始编码”的完整 Android App 技术方案。

禁止：
- 只输出概念性描述
- 只输出伪代码
- 忽略 Android 平台限制
- 使用过时方案（如 ListView、AsyncTask）
- 输出无法运行的 Demo 风格代码

要求：
- 所有代码基于现代 Android 开发规范
- 所有架构必须可扩展
- 所有功能必须考虑真实设备运行限制

--------------------------------------------------
# App 信息

应用名称：GoalWall

平台：
- Android Only
- 最低支持 Android 10
- Target Android 15

开发语言：
- Kotlin

UI 技术：
- Jetpack Compose

架构：
- MVVM + Clean Architecture

数据库：
- Room(SQLite)

依赖注入：
- Hilt

异步：
- Kotlin Coroutines + Flow

状态管理：
- StateFlow

本地通知：
- WorkManager + NotificationManager

桌面小组件：
- AppWidget + Glance（优先）
- 如 Glance 不适合，可使用 RemoteViews

图片生成：
- Canvas + Bitmap

--------------------------------------------------
# 任务目标

生成一个完整 Android App 方案与核心代码。

要求输出内容具备：

- 可直接开始开发
- 可直接建立工程目录
- 可直接复制关键代码
- 可明确模块职责
- 可明确状态同步机制
- 可明确 Widget 更新机制

输出深度必须达到：
“高级 Android 工程师项目设计文档”级别。

--------------------------------------------------
# 用户故事

作为一个希望每天完成多个小目标的用户，我希望：

- 在 App 中为每一天创建多个小目标
- 每个目标具有：
  - 未开始
  - 进行中
  - 已完成
  三种状态

- 当某一天所有目标都完成后：
  系统自动完成当天打卡

- 每晚固定时间提醒我：
  “编写明天的目标”

- 点击通知后：
  自动跳转到“明日目标编辑页”

- 我希望：
  手机上的桌面 Widget 能实时显示当天目标状态

- Widget 中：
  必须显示：
  - 当前日期
  - 完成进度
  - 每个目标状态

- 当我修改目标状态后：
  Widget 必须立即刷新同步

- 提供：
  “生成静态壁纸”功能

--------------------------------------------------
# 功能需求

## 1. 每日目标系统

支持：

- 新增目标
- 编辑目标
- 删除目标
- 拖拽排序
- 修改状态
- 一键复制昨日目标到今日

目标状态：

0 = 未开始
1 = 进行中
2 = 已完成

--------------------------------------------------
## 2. 自动打卡系统

逻辑：

当用户修改任意目标状态后：

检查：
当天是否所有目标都 status == 2

如果：
- 全部完成
- 且今天未打卡

则：
- 自动插入打卡记录
- 自动更新连续打卡天数

要求：
- 防止重复打卡
- 必须事务安全
- 必须考虑跨天情况

--------------------------------------------------
## 3. 夜间通知提醒

默认：
每天 21:00

通知内容：

✍️ 编写明天的目标，让每一天更高效！

点击通知后：

跳转：
TomorrowGoalEditScreen

要求：

- 使用 WorkManager
- 支持用户修改提醒时间
- 支持 Android 13+ 通知权限
- 考虑系统重启后的重新注册

--------------------------------------------------
## 4. Widget（核心）

采用：

AppWidget + Glance

支持尺寸：

- 2x2
- 4x2

Widget 必须显示：

- 当前日期
- 今日完成进度（3/5）
- 目标列表
- 状态图标

状态视觉要求：

未开始：
- 灰色圆点

进行中：
- 黄色进行图标

已完成：
- 绿色对勾

--------------------------------------------------
## 5. Widget 同步机制（重点）

当用户修改目标状态：

流程：

1. 更新 Room 数据库
2. Repository 发出 Flow 更新
3. ViewModel 更新 StateFlow
4. WidgetSyncManager 被触发
5. 调用：
   GlanceAppWidget.updateAll()

要求：

- 给出完整同步链路代码
- 说明为何能立即刷新
- 说明 Android Widget 刷新限制
- 避免高频刷新导致耗电

--------------------------------------------------
## 6. 静态壁纸功能

用户点击：

“生成壁纸”

App 自动：

- 将当前目标渲染成 Bitmap
- 保存到相册

要求：

- 使用 Canvas 绘制
- 深色模式适配
- 给出核心 Bitmap 生成代码

--------------------------------------------------
## 7. 统计功能

提供：

- 连续打卡天数
- 月度打卡日历
- 完成率统计

要求：

- Compose 日历视图
- 简洁 UI

--------------------------------------------------
# 数据库设计

必须输出：

1. Room Entity
2. DAO
3. Repository
4. Migration 策略

数据表：

## Goal

字段：

- id
- title
- status
- orderIndex
- date
- createdAt
- updatedAt

## CheckinRecord

字段：

- id
- date
- allCompletedFlag
- completedAt

--------------------------------------------------
# 项目结构（必须输出）

必须给出完整目录结构：

例如：

app/
 ├── data/
 ├── domain/
 ├── ui/
 ├── widget/
 ├── notification/
 ├── wallpaper/
 ├── worker/
 ├── di/
 ├── utils/

并解释：
每层职责。

--------------------------------------------------
# UI 要求

风格：

- 极简
- 深色模式
- 类似：
  - Google Tasks
  - TickTick
  - Notion Minimal Style

必须输出：

- Compose 页面代码
- Scaffold 结构
- LazyColumn
- 状态切换组件
- 动画效果

页面：

1. 首页
2. 每日目标编辑页
3. 统计页
4. 设置页

--------------------------------------------------
# 必须输出的内容（严格按顺序）

## Phase 1
整体技术架构

## Phase 2
项目目录结构

## Phase 3
数据库设计
- Entity
- DAO
- Repository
- Migration

## Phase 4
MVVM 状态流设计

## Phase 5
Compose UI 实现
- 首页
- 编辑页
- 统计页
- 设置页

## Phase 6
Widget 实现
- Glance
- 数据同步
- 点击事件
- 刷新机制

## Phase 7
自动打卡逻辑

## Phase 8
通知系统
- WorkManager
- Notification Channel
- DeepLink 跳转

## Phase 9
静态壁纸实现
- Bitmap
- Canvas
- 保存相册

## Phase 10
完整状态同步流程

## Phase 11
Android 平台限制说明

## Phase 12
未来扩展能力
- 云同步
- Firebase
- WearOS
- 锁屏 Widget

--------------------------------------------------
# 输出质量要求（非常重要）

输出必须：

- 工程化
- 可运行
- 非 Demo 化
- 有真实 Android 项目结构
- 有真实代码
- 有类名
- 有函数名
- 有 Flow 链路
- 有 ViewModel
- 有 Repository
- 有 Widget 刷新代码
- 有 Room 查询代码

禁止：

- “这里略”
- “开发者自行实现”
- “简单处理”
- 空泛描述

--------------------------------------------------
# 特别注意（必须遵守）

1. Android Widget 不允许真正实时秒级刷新
必须说明系统刷新限制

2. Widget 点击交互有限
必须说明哪些功能能做、哪些不能做

3. Android 13+ 通知权限必须处理

4. 必须考虑：
- App 被杀死
- 系统重启
- 时区变化
- 跨天切换
- Widget 多实例
- 深色模式

5. 所有状态更新：
必须单向数据流

--------------------------------------------------
# 最终目标

输出结果必须达到：

“一个 Android 工程师可以直接依据该方案开始完整开发 GoalWall App”
